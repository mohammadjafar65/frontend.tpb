# public_html/admin.thepilgrimbeez.com/.htaccess  — SPA at /tpb-admin/build

<IfModule mod_rewrite.c>
  RewriteEngine On

  # Force HTTPS for the admin subdomain
  RewriteCond %{HTTPS} !=on
  RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

  # Map all requests to /tpb-admin/build/ unless already there
  RewriteCond %{REQUEST_URI} !^/tpb-admin/build/
  RewriteRule ^(.*)$ /tpb-admin/build/$1 [L]

  # Serve real files/folders directly
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # SPA fallback
  RewriteRule ^ /tpb-admin/build/index.html [L]
</IfModule>

Options -Indexes
