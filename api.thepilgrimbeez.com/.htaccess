# public_html/api.thepilgrimbeez.com/.htaccess  — Node app (Passenger)

# Passenger must stay enabled here (CloudLinux Node.js App)
PassengerAppType node
PassengerStartupFile server.js
PassengerBaseURI /
# If you know the absolute path, uncomment the next line and set it:
# PassengerAppRoot /home/<ACCOUNT>/public_html/api.thepilgrimbeez.com

# Friendlier startup errors in the browser if something crashes
PassengerFriendlyErrorPages on

# If your app needs a specific Node version, CloudLinux usually injects it.
# Otherwise you can add (ask host for exact path):
# PassengerNodejs /opt/alt/alt-nodejs18/root/usr/bin/node